apiVersion: v1
kind: Service
metadata:
  name: prt-app-service
spec:
  type: NodePort
  selector:
    app: prt-web
  ports:
  - protocol: TCP
    port: 80         # Internal cluster port
    targetPort: 80   # Container port
    nodePort: 32000  # Exposed port external users hit on your k8s-node
